services:
  - type: web
    name: strike-zone
    runtime: node
    plan: free
    region: oregon
    buildCommand: npm install --omit=dev
    startCommand: node server.js
    healthCheckPath: /health
    autoDeploy: true
    envVars:
      - key: NODE_ENV
        value: production
      - key: RENDER
        value: "true"
      # Opcional: Neon Postgres grátis — contas não somem quando o Render dorme
      # - key: DATABASE_URL
      #   sync: false
