{
  "name": "strike-zone",
  "version": "1.0.0",
  "description": "Strike Zone — FPS tático no navegador",
  "private": true,
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "node server.js",
    "apk:sync": "cap sync android",
    "apk:debug": "cd android && gradlew.bat assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^8.4.0",
    "@capacitor/cli": "^8.4.0",
    "@capacitor/core": "^8.4.0",
    "stripe": "^22.2.1"
  }
}
